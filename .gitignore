
# NOTE: 原 PHC 用裸 `*.json` 忽略所有 json。
# 我们要用 git 传实验数据（含 metrics.json 等小文件），故改为只忽略
# 数据型 json（AMASS/mocap 元数据），放开 config/metrics 类 json。
data/**/*.json

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
out/
# C extensions
*.so
*.pkl

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
wandb/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

.vscode/*
.vscode/settings.json



# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/


# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
venv/
env.bak/
venv.bak/
 

# Rope project settings
.ropeproject

# mkdocs documentation
/site

 # Pyre type checker
.pyre/

# === 权重 / 大数据：不传 git（太大），用 rsync / 云盘单独存 ===
checkpoints/
*.pt
*.pkl
*.ckpt
# AMASS / mocap 等大数据目录（PHC 下载到 sample_data/；amass 仓库另放）
sample_data/
# 注意：data/* 已删除——它会误伤 phc/data/cfg/ 和 phc/data/assets/（这些要传）。
# 如有大数据子目录需忽略，在此精确列出（例：data/amass/）。
# data/amass 服务器上是软链接到 /root/autodl-tmp/amass，gitignore 目录规则 data/amass/ 不匹配符号链接，
# 故同时列 data/amass（无尾斜杠）匹配链接本身。
data/amass/
data/amass
data/raw/
# SMPL 模型文件（.npz/.zip），PHC 运行时下载到 data/smpl/，不进 git。
data/smpl/

# === 实验输出目录：放开（原 PHC 忽略 output/ log/ out/，我们要用 git 传实验数据）===
# output/ 由 PHC 用作训练输出 + 预训练 checkpoint 落地，其中 .pt/.pkl 已被上面忽略，
# 其余 config 副本 / metrics / 日志等小文件允许提交。
# （如需排除 output 下的某些子目录，在此精确列出。）

# 日志文件（按文件名忽略，目录放开）
*.log
stageroom_train/ # tensorboard log
trials/
new_trials

# 图片 / 视频：默认不传（体积大，走 rsync）；如需提交 demo 截图可注释掉
*.png
*.jpg
*.mp4
*.gif

gh-md-toc
# === AAA: 大产物 / 本地镜像不进 PHC git（master_plan 两层 git 关系）===
# output/ = 训练 checkpoint（2.9G+），data/ 已按需忽略 json/pkl，aaa_w3/ = code/ 的 scp 镜像，
# repro_logs/ = 复现日志，*.bak = 服务器侧备份。这些只在服务器存在，正式代码以 phc/ 副本进 git。
output/
aaa_w3/
repro_logs/
*.bak
# AAA 运行时副本：正式副本在 AAA 仓（code/scripts/ + data/style/），服务器这份是 scp/训练副本不进 PHC git。
# 注意只列 data/style/，不要写裸 data/——phc/data/cfg/ 和 phc/data/assets/ 要进 git。
aaa_w5_diag.py
run_harder_baseline.sh
data/style/
# AAA watcher/n ihm 运行副本：正式副本在 AAA 仓 code/scripts/，服务器根目录这些 .sh/.nohup 是 scp/运行副本不进 PHC git。
# phc/learning/ 下的 .py（aaa_w4_style_sweep.py / gate0_action_response.py）是 run_hydra import 需要的正式副本，要 tracked，不在此列。
*_watch.sh
diagA_summary.sh
*.nohup
# === AAA end ===
